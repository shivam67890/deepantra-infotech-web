# Copy to .env in the project root (same level as package.json)
VITE_API_URL=http://localhost:5000/api
+# Shows the "Admin: Bookings" link in the navbar for this email only.
+# This is cosmetic (UI visibility) — the real access check happens on the
+# backend via ADMIN_EMAILS in server/.env. Should match one of those emails.
+VITE_ADMIN_EMAIL=deepantrainfotech@gmail.com